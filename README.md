# 注意事项

在运行项目前，**必须**先执行 `dbs_init.sql` 脚本以完成数据库的初始化。

## 如何运行 `dbs_init.sql` 脚本？

1. 打开你的数据库管理工具（例如MySQL Workbench, pgAdmin，等等）。
2. 在SQL执行窗口中打开 `dbs_init.sql` 文件。
3. 运行整个脚本。

执行完上述步骤后，你的数据库应已准备好，可以正常运行项目了。

确保正确执行此步骤，以避免任何数据库相关的运行错误。

感谢你的配合！
